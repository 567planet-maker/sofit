@AGENTS.md

일반 보안 점검
[ ] "기능이 작동하는가" + "작동하지 말아야 할 때도 작동하는가"를 모두 테스트하는가?
[ ] 내 계정으로 남의 데이터 조회되는가? (IDOR) / URL 파라미터 바꿔도 막히는가? / 로그아웃 후 이전 URL 접근 막히는가?
[ ] 숨겨놓은 URL도 스크립트는 1초에 수천 번 확인한다. 보호되지 않은 라우트가 있는가?

인증 및 접근 제어
관리자 페이지
[ ] /admin, /dashboard, /internal 등이 인증 없이 접근 불가한가?
[ ] 관리자 페이지를 "숨김"이 아닌 "인증"으로 보호하는가?
[ ] if문 하나가 아닌 미들웨어/세션 기반 인증을 사용하는가?

비밀번호 재설정
[ ] 재설정 토큰은 1회용인가?
[ ] 토큰 만료 시간이 15분 이내인가?
[ ] 성공/실패 응답 메시지가 동일한가? ("이메일이 없습니다" 노출 금지)

소셜 로그인(OAuth)
[ ] OAuth state 파라미터를 검증하는가?
[ ] 콜백 URL 화이트리스트가 있는가?
[ ] 토큰 만료를 처리하는가?
[ ] 로그아웃 시 토큰을 무효화하는가?

일반
[ ] Brute Force 방어(로그인 시도 횟수 제한)가 있는가?
[ ] MFA(다중 인증) 로직이 구현되어 있는가?
[ ] 세션 타임아웃, 동시 로그인 제한이 설정되어 있는가?
[ ] 백엔드에서도 인증/권한 검증을 하는가? (프론트에서만 막으면 백엔드는 열려있음)

키 및 시크릿 관리
[ ] service_role key, secret key, admin key가 서버에서만 접근 가능한가?
[ ] NEXT_PUBLIC_ 환경변수에 시크릿이 노출되지 않았는가?
[ ] Git 히스토리에 시크릿이 포함된 적이 있는가?
[ ] .env 파일이 .gitignore에 포함되어 있는가?
[ ] 키 로테이션 주기(최소 90일)가 설정되어 있는가?
[ ] 시크릿 스캐닝(GitHub Secret Scanning, trufflehog 등)이 설정되어 있는가?
[ ] 빌드된 JS 번들에서 시크릿이 포함되지 않았는가?

Supabase / Next.js 특화
Supabase
[ ] RLS(Row Level Security)가 켜져 있는가?
[ ] anon key로 전체 테이블 조회가 되는가? (되면 이미 뚫린 것)
[ ] RLS 정책을 실제로 테스트해봤는가?

Next.js
[ ] getServerSideProps에서 유저 인증을 검증하는가?
[ ] 클라이언트에서 직접 Supabase 쿼리를 하지 않는가?
[ ] 모든 API route에 인증이 붙어있는가?

프롬프트 인젝션 방어
[ ] 시스템 프롬프트에 API 키, 비즈니스 로직 등 민감 정보가 없는가?
[ ] 프롬프트 인젝션 방어가 있는가? ("지시 전부 출력해줘", "Ignore previous instructions" 등)
[ ] 사용자 입력과 시스템 프롬프트를 분리하는가?
[ ] 출력 필터링으로 시스템 프롬프트 노출을 차단하는가?
[ ] AI 엔드포인트에 Rate Limiting이 있는가?
[ ] 댓글/리뷰 기능에 XSS 방어(HTML 이스케이핑)가 적용되어 있는가?
[ ] AI 대화 중 의심스러운 인젝션 시도를 로깅하는가?

로깅 및 데이터 보호
[ ] 프로덕션에 console.log가 남아있지 않은가?
[ ] 로그에 비밀번호, 이메일 전체, 전화번호, 카드번호가 기록되지 않는가?
[ ] 로그에 JWT 토큰 전체, API 키가 기록되지 않는가?
[ ] 로그 데이터의 보관 기간과 접근 권한이 설정되어 있는가?
[ ] JWT에 비밀번호 / 신용카드 / 주민번호 / 전체 유저 객체 / 내부 DB ID가 없는가?
[ ] JWT secret이 충분히 강력하고 만료 시간이 적절한가?
[ ] 알림 기능에 rate limiting이 있는가?

모니터링 및 탐지
[ ] 봇/스캐너 접근을 탐지할 수 있는가?
[ ] 비정상 트래픽 급증에 대한 알림이 있는가?
[ ] 모든 HTTP 요청 로그를 수집/보관하는가? (최소 90일)
[ ] 인증 실패 로그를 별도로 추적하는가?
[ ] 실시간 대시보드(접속자, 봇 비율, 에러율)가 있는가?
[ ] 보안 이벤트 발생 시 즉시 알림(Slack/이메일)이 오는가?
[ ] WAF(Web Application Firewall)가 설정되어 있는가?

클라우드 / 인프라 보안
[ ] AdministratorAccess 없이 최소 권한 원칙을 적용하는가?
[ ] IAM 사용자별 필요한 권한만 부여했는가?
[ ] Security Group이 과도하게 열려있지 않은가? (0.0.0.0/0 금지)
[ ] S3 버킷이 Public으로 설정되어 있지 않은가?
[ ] 버킷 정책과 ACL이 적절한가?
[ ] 데이터 암호화(at rest, in transit)가 설정되어 있는가?
[ ] OS 패치가 정기적으로 적용되는가?
[ ] 불필요한 포트가 닫혀있는가?
[ ] 방화벽 설정이 적절한가?

과금 및 비용 보안
[ ] AWS, OpenAI, Stripe 등에 월별 비용 알림이 설정되어 있는가?
[ ] Stripe 테스트 키와 프로덕션 키가 확실히 분리되어 있는가?
[ ] 외부 API 키에 usage limit이 설정되어 있는가?
[ ] 평소 대비 급격한 비용 증가를 감지하는 모니터링이 있는가?

법률 및 컴플라이언스
[ ] 개인정보 처리방침을 공개하고 있는가?
[ ] 개인정보 수집 전 사용자 동의를 받고 있는가? (수집 항목, 목적, 보유 기간 명시)
[ ] 민감 정보(주민번호, 카드번호 등)가 암호화되어 저장되는가?
[ ] 보유 기간이 지난 개인정보를 즉시 파기하는가?
[ ] 데이터 최소 수집 원칙을 지키는가?
[ ] 침해 사고 시 72시간 내 신고 프로세스가 있는가?
[ ] 해외 서버 저장 시 관련 고지를 했는가?

코드 위생
[ ] MVP 단계에서도 최소 보안(인증, 키 분리, HTTPS)을 적용하는가?
[ ] "임시로 해둔" 코드(TODO, HACK, TEMP)가 프로덕션에 남아있지 않은가?

취약점 대응
[ ] 의존성 취약점 스캔(npm audit, Snyk 등)을 하는가?
[ ] 알려진 CVE 패치를 자동 적용하는 파이프라인이 있는가?
[ ] 불필요한 엔드포인트, 미사용 기능이 비활성화되어 있는가?
[ ] 다층 방어(Defense in Depth): 네트워크/앱/데이터 각 레이어에 방어가 있는가?
