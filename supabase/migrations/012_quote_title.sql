-- ============================================================
-- SOFIT — 견적 요청 제목(title) 컬럼 추가
-- 사용자가 견적을 제목으로 구분(특히 여러 임시저장)할 수 있게 한다.
-- Supabase SQL 에디터에서 실행.
-- ============================================================

ALTER TABLE public.quote_requests
  ADD COLUMN IF NOT EXISTS title text;
