-- ============================================================
-- SOFIT — 공장 견적서에 "견적 상세 내용"(scope) 추가 (Phase 8)
-- 비용(금액)뿐 아니라 시공 범위·포함 항목·조건 등 견적 내용을 담고,
-- 수정 견적서(버전)에서도 함께 수정 가능하게 한다.
-- 비파괴(컬럼만 추가). Supabase SQL 에디터에서 전체 실행.
-- ============================================================

ALTER TABLE public.factory_quotes
  ADD COLUMN IF NOT EXISTS scope text;
